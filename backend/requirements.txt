flask==3.0.0
flask-cors==4.0.0
pillow>=10.0.0
numpy>=1.23.0
onnxruntime>=1.16.0
torch>=2.0.0
torchvision>=0.15.0
h5py>=3.8.0          # Required to load MobileNetV2 .keras (HDF5) checkpoint
opencv-python>=4.8.0
# For PDF generation (frontend dependency, but good to note)
# jspdf is handled via npm