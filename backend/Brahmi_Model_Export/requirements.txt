# ============================================================
# Brahmi GAN Restoration — Python Dependencies
# Install with: pip install -r requirements.txt
#
# IMPORTANT: Install PyTorch with CUDA FIRST (before this file):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# ============================================================

torch>=2.2.0          # must have CUDA wheel installed separately (see above)
torchvision>=0.17.0
numpy>=1.24.0
Pillow>=10.0.0
opencv-python>=4.8.0
matplotlib>=3.7.0
